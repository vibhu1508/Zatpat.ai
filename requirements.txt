# =============================================================================
# Zatpat.ai Production Dependencies (Optimized for <512MB RAM)
# =============================================================================

# Dataset & HuggingFace
huggingface_hub
requests

# Embeddings (PyTorch CPU / Sentence-Transformers)
sentence-transformers
numpy

# Redis Stack client (Vector search + Session memory)
redis[hiredis]

# LLM client (Ollama)
ollama

# Web server & WebSockets
fastapi
uvicorn[standard]
websockets

# Environment & utilities
python-dotenv
