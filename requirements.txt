# =============================================================================
# Zatpat.ai Cloud Production Dependencies (CPU-Only, <80MB RAM)
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cpu

# PyTorch CPU-Only (no heavy NVIDIA CUDA packages)
torch
sentence-transformers
numpy

# Redis Stack client
redis[hiredis]

# Web server & WebSockets
fastapi
uvicorn[standard]
websockets

# LLM & STT clients
ollama
requests
python-dotenv
huggingface_hub
